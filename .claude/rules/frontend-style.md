## Frontend Architecture

### Stack

React 18, TypeScript, Vite, MUI (Material UI), React Router v6, TanStack React Query, Axios, Notistack, react-hook-form + Zod, Zustand.

### Path Alias

`@/` maps to `src/`. **Always use `@/` for imports, never relative paths.**

